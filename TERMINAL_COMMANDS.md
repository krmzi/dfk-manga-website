# 🎯 أوامر Terminal - نفذها بالترتيب

## الخطوة 1: إيقاف الـ Server الحالي
اضغط `Ctrl+C` في Terminal

## الخطوة 2: مسح الـ Cache
```powershell
Remove-Item -Recurse -Force .next
```

## الخطوة 3: إعادة تشغيل التطبيق
```powershell
npm run dev
```

## الخطوة 4: انتظر حتى ترى
```
✓ Ready in X.Xs
○ Local: http://localhost:3000
```

---

## ملاحظات مهمة:

1. **لا تنسى تطبيق ملف SQL في Supabase أولاً!**
   - افتح: https://supabase.com/dashboard
   - SQL Editor → نفذ ملف `RUN_THIS_NOW.sql`

2. **بعد تطبيق SQL، نفذ الأوامر أعلاه**

3. **اختبر النظام:**
   - افتح: http://localhost:3000
   - سجل دخول: dfk_admin2002@gmail.com
   - تحقق من ظهور زر "لوحة التحكم"

---

## إذا واجهت مشكلة في Terminal:

**خطأ: "Remove-Item: Cannot find path"**
```powershell
# تجاهل الخطأ وأكمل
npm run dev
```

**خطأ: "Port 3000 is already in use"**
```powershell
# أوقف العملية القديمة
Get-Process -Name node | Stop-Process -Force
# ثم أعد التشغيل
npm run dev
```

---

## ✅ كل شيء جاهز!

الكود تم تحديثه بالكامل مع:
- ✓ Retry Logic (3 محاولات)
- ✓ Caching (5 دقائق)
- ✓ Fallback للـ Super Admin
- ✓ Error Handling شامل

**فقط نفذ الأوامر وستعمل كل شيء! 🚀**
