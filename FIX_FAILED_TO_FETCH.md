# 🚨 حل مشكلة "Failed to fetch" - خطوات إصلاح عاجلة

## المشكلة:
الموقع يعرض أخطاء "Failed to fetch" و "AuthRetryableFetchError" لأن الاتصال بقاعدة بيانات Supabase غير صحيح أو مفقود.

## الحل السريع (5 دقائق):

### الخطوة 1: التحقق من ملف .env.local
افتح ملف `.env.local` في جذر المشروع وتأكد من وجود هذين السطرين:

```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

### الخطوة 2: الحصول على بيانات Supabase الصحيحة
1. اذهب إلى: https://supabase.com/dashboard
2. اختر مشروعك (أو أنشئ مشروع جديد إذا لم يكن لديك)
3. من القائمة الجانبية: **Settings** → **API**
4. انسخ:
   - **Project URL** → ضعه في `NEXT_PUBLIC_SUPABASE_URL`
   - **anon/public key** → ضعه في `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### الخطوة 3: إعادة تشغيل السيرفر
بعد تحديث ملف `.env.local`:
1. أوقف السيرفر الحالي (اضغط `Ctrl + C` في التيرمينال)
2. شغل السيرفر من جديد: `npm run dev`

### الخطوة 4: مسح Service Worker القديم
1. افتح الموقع: http://localhost:3000
2. اضغط `F12` لفتح DevTools
3. اذهب إلى تبويب **Application**
4. من القائمة الجانبية: **Service Workers**
5. اضغط **Unregister** لكل service worker موجود
6. أعد تحميل الصفحة بـ `Ctrl + Shift + R`

### الخطوة 5: إنشاء الجداول في Supabase
إذا كانت قاعدة البيانات فارغة، نفذ هذه الملفات بالترتيب في Supabase SQL Editor:

1. **COMPLETE_FIX.sql** - لإنشاء جميع الجداول
2. **supabase_fix_permissions.sql** - لضبط الصلاحيات

## التحقق من نجاح الإصلاح:
- افتح http://localhost:3000
- يجب ألا ترى أي أخطاء في Console
- إذا كانت قاعدة البيانات فارغة، ستظهر رسالة "المكتبة هادئة حالياً" (هذا طبيعي)
- يمكنك إضافة مانجا من صفحة الأدمن: http://localhost:3000/admin

## إذا استمرت المشكلة:
1. تأكد من أن مشروع Supabase نشط (ليس متوقف)
2. تأكد من عدم وجود مسافات زائدة في ملف .env.local
3. تأكد من أن الـ URL يبدأ بـ `https://` وينتهي بـ `.supabase.co`
4. جرب إنشاء مشروع Supabase جديد إذا كان المشروع القديم معطوب

---
تاريخ الإنشاء: 2025-12-05
الإصدار: 1.0
