# 🔧 إصلاح مشكلة إغلاق صفحات المانجا بالخطأ

## المشكلة التي كانت موجودة

البوت كان يغلق صفحات المانجا **قبل** أن تتحمل، ظناً منه أنها إعلانات!

### لماذا حدث هذا؟

```python
# الكود القديم (المشكلة):
browser.get(url)
cleanup_tabs(browser)  # ❌ يغلق التبويبات فوراً!
```

المشكلة: `cleanup_tabs` كانت تعمل **فوراً** بعد `browser.get()`، والصفحة لم تتحمل بعد!

## الحل الذي تم تطبيقه

### 1. إضافة انتظار قبل التنظيف
```python
# الكود الجديد (الحل):
browser.get(url)
time.sleep(2)  # ✅ ننتظر الصفحة تتحمل
cleanup_tabs(browser)  # الآن آمن!
```

### 2. تحسين دالة cleanup_tabs
الآن الدالة:
- ✅ تنتظر ثانية واحدة لتحميل التبويبات
- ✅ تفحص URL كل تبويب قبل الإغلاق
- ✅ تحتفظ بالتبويبات التي تحتوي على:
  - `olympustaff.com`
  - `lekmanga.net`
  - `challenges.cloudflare`
- ✅ تغلق فقط التبويبات المؤكد أنها إعلانات

### 3. إزالة التنظيف المبكر
```python
# القديم (خطأ):
while True:
    cleanup_tabs(browser)  # ❌ لا توجد صفحات محملة بعد!
    # ... بقية الكود

# الجديد (صحيح):
while True:
    # لا تنظيف هنا!
    # ... نفتح الصفحات أولاً
    browser.get(url)
    time.sleep(2)
    cleanup_tabs(browser)  # ✅ الآن فقط!
```

## كيف تجرب الكود الآن

```powershell
python dfk_r2_bot_fixed.py
```

## ما يجب أن تراه

```
[21:XX:XX] [SCAN] Scanning: https://olympustaff.com/series/...
[21:XX:XX] [CLEAN] Closing 2 ad tabs (keeping 1 manga tabs)
[21:XX:XX] [SCAN] Found 45 chapters on source.
```

## إذا استمرت المشكلة

أرسل لي:
1. لقطة شاشة من Terminal
2. هل ترى رسالة "Closing X ad tabs"؟
3. كم عدد التبويبات المفتوحة في Chrome؟

---

**ملاحظة مهمة**: هذا الكود الآن يعتمد على الكود القديم الذي كان يعمل 100%، مع إصلاح مشكلة التوقيت فقط!
